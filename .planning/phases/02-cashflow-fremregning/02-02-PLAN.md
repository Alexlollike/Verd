---
phase: 02-cashflow-fremregning
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - verd/__init__.py
  - examples/eksempel_cashflow.py
autonomous: false
requirements:
  - CASH-05

must_haves:
  truths:
    - "fremregn er importerbar direkte fra verd-pakken (from verd import fremregn)"
    - "examples/eksempel_cashflow.py koerer uden fejl og printer en laesbar cashflow-tabel"
    - "Tabellen viser t, alder, sandsynlighed_i_live, depot_dkk og indbetaling_dkk for de foerste 24 maaneder"
    - "Tabellen slutter med totalt antal raekker og samlet indbetaling"
  artifacts:
    - path: "verd/__init__.py"
      provides: "fremregn eksporteret fra pakken"
      contains: "from verd.projection import fremregn"
    - path: "examples/eksempel_cashflow.py"
      provides: "Phase 2 done-kriterium: cashflow-tabel printet for eksempelpolicy"
      exports: []
  key_links:
    - from: "examples/eksempel_cashflow.py"
      to: "verd/projection.py"
      via: "from verd.projection import fremregn (eller from verd import fremregn)"
      pattern: "import fremregn"
    - from: "examples/eksempel_cashflow.py"
      to: "examples/eksempel_police.py"
      via: "genbrug police og modeller fra eksempel_police.py (kopier konfiguration)"
      pattern: "GompertzMakeham|DeterministicMarket|Policy"
---

<objective>
Eksponér `fremregn()` i pakkens public API og demonstrér Phase 2's done-kriterium: cashflow-tabellen for eksempelpolicen kan printes fra kommandolinjen.

Purpose: `__init__.py`-eksporten lader Fase 3 og 4 importere `fremregn` direkte fra `verd` uden at kende den interne modulstruktur. `eksempel_cashflow.py` er det haandgribelige bevis paa at Fase 2 er fuldt funktionel.
Output: Opdateret `verd/__init__.py` med `fremregn` i `__all__`. Ny `examples/eksempel_cashflow.py` der printer en formatted tabel og resumé.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cashflow-fremregning/02-CONTEXT.md
@.planning/phases/02-cashflow-fremregning/02-01-SUMMARY.md

<interfaces>
<!-- Kontrakten fra Plan 01 — executor behoever ikke udforske kodebasen. -->

Fra verd/projection.py (skabt i Plan 01):
```python
DT: float = 1 / 12
MAX_ALDER: float = 110.0
P_LIVE_TAERSKEL: float = 1e-6

def fremregn(
    fordeling: PolicyDistribution,
    biometri: BiometricModel,
    marked: FinancialMarket,
) -> pd.DataFrame:
    """Returnerer DataFrame med kolonner: t, alder, sandsynlighed_i_live,
    enhedspris, depot_enheder, depot_dkk, indbetaling_dkk, risikopraemie_dkk"""
```

Fra verd/__init__.py (nuvaerende eksport):
```python
from verd.policy_state import PolicyState
from verd.policy import Policy
from verd.policy_distribution import PolicyDistribution, initial_distribution
from verd.biometric_model import BiometricModel
from verd.gompertz_makeham import GompertzMakeham
from verd.financial_market import FinancialMarket
from verd.deterministic_market import DeterministicMarket

__all__ = [
    "PolicyState", "Policy", "PolicyDistribution", "initial_distribution",
    "BiometricModel", "GompertzMakeham", "FinancialMarket", "DeterministicMarket",
]
```

Fra examples/eksempel_police.py (politikonfiguration til genbrug):
```python
biometri = GompertzMakeham(alpha=0.0005, beta=0.00004, sigma=0.09)
marked = DeterministicMarket(r=0.05, enhedspris_0=100.0)
police = Policy(
    foedselsdato=date(1980, 1, 15), tegningsdato=date(2020, 6, 1),
    pensionsalder=67, er_under_udbetaling=False,
    gruppe_id="DK_MAND_2023", omkostningssats_id="STANDARD",
    loen=600_000.0, indbetalingsprocent=0.15,
    aldersopsparing=1_200.0, ratepensionsopsparing=800.0,
    ratepensionsvarighed=10, livrentedepot=500.0,
    tilstand=PolicyState.I_LIVE,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eksportér fremregn fra verd/__init__.py</name>
  <files>verd/__init__.py</files>
  <action>
    Tilfoej `fremregn` til `verd/__init__.py`:

    1. Tilfoej import-linjen efter de eksisterende imports:
       ```python
       from verd.projection import fremregn
       ```

    2. Tilfoej `"fremregn"` til `__all__`-listen (indsaet det logisk — f.eks. efter `"initial_distribution"`).

    Det er tilstraekkeligt at eksportere `fremregn` — `DT`, `MAX_ALDER` og `P_LIVE_TAERSKEL` er interne konstanter og eksporteres fra `verd.projection` direkte.
  </action>
  <verify>
    <automated>cd "C:/Users/Alexander Lollike/OneDrive/Claudes mappe/Verd/Verd" && python -c "from verd import fremregn; print('OK: fremregn importeret fra verd:', fremregn)"</automated>
  </verify>
  <done>
    `from verd import fremregn` koerer uden fejl og returnerer funktionen.
    `"fremregn"` er i `verd.__all__`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Opret examples/eksempel_cashflow.py — Phase 2 done-kriterium</name>
  <files>examples/eksempel_cashflow.py</files>
  <action>
    Opret `examples/eksempel_cashflow.py` som demonstrerer Fase 2's done-kriterium: cashflow-tabel for eksempelpolicen printes.

    Scriptet skal:
    1. Oprette de samme biometri, marked og police som `eksempel_police.py` (kopier konfigurationen direkte ind — ikke importér fra eksempel_police.py).
    2. Kalde `initial_distribution(police)` og `fremregn(fordeling, biometri, marked)`.
    3. Printe en sektionsoverskrift med `"=" * 60`.
    4. Printe de foerste 24 raekker (2 aars fremregning) med disse kolonner formateret:
       - `t` (2 decimaler)
       - `alder` (2 decimaler)
       - `sandsynlighed_i_live` (8 decimaler)
       - `depot_dkk` (kommasepareret, 2 decimaler, DKK)
       - `indbetaling_dkk` (kommasepareret, 2 decimaler, DKK)
       Brug `df[cols].head(24).to_string(index=False)` til selve tabellen.
    5. Printe et resumé-afsnit med:
       - Totalt antal raekker i DataFrame
       - Samlet indbetaling (sum af indbetaling_dkk-kolonnen) i DKK
       - Slut-depotvaerdi (depot_dkk paa sidste raekke) i DKK
       - Slut-sandsynlighed_i_live (paa sidste raekke)
    6. Printe Phase 2 done-kriterium bekraeftelse:
       ```
       Phase 2 done-kriterium: Cashflow-tabel printet for eksempelpolicy
       ```

    Tilfoej en docstring oeverst i filen der forklarer formaaalet og hvordan man koerer scriptet:
    ```python
    """
    Eksempel: Sandsynlighedsvagtet cashflow-fremregning (Phase 2 done-kriterium).

    Demonstrerer:
      - fremregn() med en unit-link opsparingspolicy
      - Cashflow-tabel med t, alder, sandsynlighed_i_live, depot_dkk, indbetaling_dkk

    Koer med:
        python examples/eksempel_cashflow.py
    """
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Alexander Lollike/OneDrive/Claudes mappe/Verd/Verd" && python examples/eksempel_cashflow.py</automated>
  </verify>
  <done>
    Scriptet koerer uden fejl og printer:
    - En tabel med 24 raekker og 5 kolonner (t, alder, sandsynlighed_i_live, depot_dkk, indbetaling_dkk)
    - Et resumé med totalt antal raekker, samlet indbetaling og slut-depotvaerdi
    - "Phase 2 done-kriterium: Cashflow-tabel printet for eksempelpolicy"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `verd/projection.py` med `fremregn()`, `verd/__init__.py` opdateret med eksport, og `examples/eksempel_cashflow.py` der printer cashflow-tabellen.
  </what-built>
  <how-to-verify>
    1. Koer `python examples/eksempel_cashflow.py` fra projektmappen.
    2. Kontroller at tabellen viser 24 raekker med stigende `depot_dkk` og faldende `sandsynlighed_i_live`.
    3. Kontroller at `indbetaling_dkk` er ca. 7.500 DKK pr. raekke (600.000 x 15% / 12 = 7.500).
    4. Kontroller at `depot_dkk` paa raekke 1 er ca. 250.000 DKK (2500 enheder x 100 DKK) og vokser.
    5. Kontroller at resuméet viser et fornuftigt totalt antal raekker (ca. 840 = 70 aar x 12 mdr).
  </how-to-verify>
  <resume-signal>Skriv "approved" hvis tabellen ser korrekt ud, eller beskriv hvad der er forkert.</resume-signal>
</task>

</tasks>

<verification>
Koer: `python examples/eksempel_cashflow.py`

Forventede kontroller:
1. Ingen Python-fejl
2. Tabel med 24 raekker er laesbar
3. depot_dkk stiger fra start til raekke 24 (baade indbetaling og afkast bidrager til DKK-vaerdi)
4. sandsynlighed_i_live falder fra 1.0 ved t=0
5. indbetaling_dkk er konsekvent ca. 7.500 DKK
6. Resuméet angiver totalt antal raekker > 800
</verification>

<success_criteria>
- `from verd import fremregn` virker
- `python examples/eksempel_cashflow.py` koerer og printer en laesbar cashflow-tabel
- Menneskeligt godkendt via checkpoint
- Phase 2 done-kriterium opfyldt: "Kan printe cashflow-tabel for en eksempelpolicy"
</success_criteria>

<output>
Efter afslutning, opret `.planning/phases/02-cashflow-fremregning/02-02-SUMMARY.md` med:
- Hvad der blev implementeret (__init__.py eksport, eksempel_cashflow.py)
- Output fra det koerte eksempel (resumétal: antal raekker, samlet indbetaling, slut-depot)
- Menneskelig godkendelse bekraeftet
- Phase 2 complete-status
</output>
